# Live-Server-App
+ Get IP for to test website on connected device on same WiFi
  ```bash
  ip addr # use it on terminal ( Android - TermUX / Ubuntu - Terminal )
  ```
  ```bash
  http://ip:port/index.html
  ```
